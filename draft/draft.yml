#  - PK: asset_id int NOT NULL
#  - name char(64) NOT NULL
#  - FK: owner_id int NOT NULL
#  - amount int NOT NULL
#  - description text
#  - max_yearly_return_rate numeric NOT NULL
#  - min_yearly_return_rate numeric NOT NULL
#  - start_age small int NOT NULL
#  - end_age small int
#  - created_at date NOT NULL```

# This is the schema for Table Assets, revised and write as OpenAPI Spec Schema
components:
  schemas:
    Risk:
      type: object
      required:
        - id
        - name
        - owner_id
        - max_loss
        - min_loss
        - start_age
        - created_at
      properties:
        id:
          type: integer
          format: int32
          description: "Primary key of the risk"
          example: 1
        name:
          type: string
          maxLength: 64
          description: "Name of the asset"
          example: "General Daily Risk"
        owner_id:
          type: integer
          format: int32
          description: "Foreign key referencing the owner"
          example: 101
        max_loss:
          type: number
          format: float
          description: "Maximum possible loss due to the risk"
        min_loss:
          type: number
          format: float
          description: "Minimum possible loss due to the risk"
        description:
          type: string
          description: "Detailed description of the risk"
          nullable: true
          example: "General Daily Risk, like losing or breaking things, or getting colds"
        start_age:
          type: integer
          format: int16
          description: "The starting age when this risk applies"
          example: 25
        end_age:
          type: integer
          format: int16
          nullable: true
          description: "The ending age when this risk applies, null if indefinite"
          example: 65
        created_at:
          type: string
          format: date
          description: "Date when the risk record was created"
          example: "2025-02-26"
